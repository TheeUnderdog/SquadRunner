# SquadRunner tmux config

# Use Ctrl-a as prefix
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Enable mouse
set -g mouse on

# Increase scrollback
set -g history-limit 50000

# Easy reload
bind r source-file ~/.tmux.conf \; display "Reloaded"